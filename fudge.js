/**
 * Fudge
 * Text Effects!
 */

(function( $ ){

  $.fn.fudge = function( options ){

      // Options
      var settings = $.extend({
          'effect': '3d'
      }, options);
      
      // Utility Functions
      var wrapper = function(id, whatever, wheatever){
      };

      // Individual Effects
      var threeD = function(){
          alert('hi');
          wrapper(asdf asdf asdf);
      },
          clotheslines = function(){
          },
          
          alarmClock = function(){
          };
      

      switch(settings.effect){
          case '3d': threeD();
              };                  

  };
})( jQuery );

   
$('h1').fudge('alarmClock');